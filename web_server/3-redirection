#!/usr/bin/env bash
# 3-redirection: Install Nginx and configure /redirect_me redirection

REDIRECT_URL="https://www.youtube.com/watch?v=QH2-TGUlwu4"
NGINX_CONF="/etc/nginx/sites-available/default"

# Update packages
sudo apt-get update -y

# Install Nginx if missing
if ! command -v nginx >/dev/null 2>&1; then
    sudo apt-get install -y nginx
fi

# Backup config
if [ ! -f "${NGINX_CONF}.bak" ]; then
    sudo cp "$NGINX_CONF" "${NGINX_CONF}.bak"
fi

# Remove old redirect
sudo sed -i '/location \/redirect_me {/,/}/d' "$NGINX_CONF"

# Insert new redirect inside server block after server_name
sudo sed -i "/server_name _;/a location /redirect_me {\n    return 301 $REDIRECT_URL;\n}" "$NGINX_CONF"

# Test and reload Nginx
sudo nginx -t
sudo service nginx reload

# Wait a moment to ensure Nginx has reloaded
sleep 1

# Output ONLY the HTTP status code (no newline)
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1/redirect_me
