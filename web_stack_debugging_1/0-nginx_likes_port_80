#!/usr/bin/env bash
# Install and start Nginx so that it listens on port 80 in a container

apt-get update -y
apt-get install -y nginx

# Remove default PID file if it exists (prevents Nginx from refusing to start)
rm -f /run/nginx.pid

# Start Nginx in the foreground
nginx -g 'daemon off;' &
