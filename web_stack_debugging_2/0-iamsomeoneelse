#!/usr/bin/env bash
# Run the whoami command as the user passed as the first argument

if [ -z "$1" ]; then
    echo "Usage: $0 <username>"
    exit 1
fi

# Run whoami as the specified user (works even if user has no home directory)
su "$1" -c "whoami"
